version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant_biotwin
    ports:
      - "6333:6333"
    volumes:
      - qdrant_storage:/qdrant/storage
    environment:
      QDRANT_SNAPSHOTS_SCHEDULER_INSTANT_SNAPSHOTS: "true"

volumes:
  qdrant_storage:
